class AppConfig {
  // Base URL for API requests
  // For development, use localhost. For production, replace with your deployed server URL (e.g., https://yourdomain.com/api)
  static const String apiBaseUrl = 'http://localhost:5000/api';
}